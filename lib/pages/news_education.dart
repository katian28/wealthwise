import 'package:flutter/material.dart';

class NewsEducationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('News and Education Page', style: TextStyle(color: Colors.white)),
      ),
    );
  }
}
